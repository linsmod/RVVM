# The root Makefile owns the rvvm_winhost target now - these are leftovers
# from the old standalone CMake harness (CMakeLists.txt / CMakePresets.json /
# mingw_toolchain.cmake) plus its runtime debug output.
build/
*.bmp
*.log
